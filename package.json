{
  "name": "nestvilla-website",
  "version": "1.0.0",
  "description": "Villa booking website for Mangrove Nest and Cozy Nest",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && set PORT=3001 && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["villa", "booking", "react", "nodejs"],
  "author": "NestVilla",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
